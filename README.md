# Сокращение ссылок

Это веб-приложение для сокращения ссылок, написанное на FastAPI.

## Стек технологий

- Backend: Python, FastAPI, SQLAlchemy
- Frontend: HTML, CSS, JavaScript
- База данных: SQLite (через SQLAlchemy)

## Структура проекта

```
├── src/
│   ├── main.py          # Точка входа приложения
│   ├── service.py       # Логика приложения
│   ├── shortener.py     # Генерация коротких ссылок
│   └── database/
│       ├── db.py        # Конфигурация базы данных
│       ├── models.py    # Модели базы данных
│       └── crud.py      # Операции с базой данных
├── tests/               # Тесты
├── frontend/            # Фронтенд-часть
│   ├── index.html       # Главная страница
│   ├── css/
│   │   └── style.css    # Стили
│   └── js/
│       └── main.js      # Логика фронтенда
├── docker-compose.yaml  # Docker Compose конфигурация
└── pyproject.toml       # Зависимости и настройки
```

## Запуск проекта

1. Убедитесь, что у вас установлен Python 3.11+ и [uv](https://github.com/astral-sh/uv) (менеджер зависимостей и виртуальных окружений).
2. Создайте и настройте виртуальное окружение с помощью `uv`:
   ```bash
   uv venv .venv
   source .venv/bin/activate  # Linux/Mac
   # или
   .venv\Scripts\activate    # Windows
3. Установите зависимости из pyproject.toml:
   ```bash
   uv pip install -e
4. Запустите сервер:
   ```bash
   uvicorn src.main:app --reload
5. Откройте фронтенд по адресу http://localhost:8000/frontend/index.html

## Использование

1. Введите длинную ссылку в поле ввода
2. Нажмите кнопку "Сократить"
3. Получите короткую ссылку, которую можно скопировать
4. Перейдите по короткой ссылке, чтобы перенаправиться на оригинальный URL

## API

- `POST /short_url` - создание короткой ссылки
- `GET /{slug}` - редирект по короткой ссылке

## Особенности

- Поддержка CORS для взаимодействия фронтенда и бэкенда
- Валидация URL на корректность
- Обработка ошибок с возвращением соответствующих HTTP-статусов
- Простой и интуитивно понятный интерфейс

## Дальнейшее развитие

- Добавление статистики по переходам
- Аутентификация пользователей
- Срок действия ссылок
- Админ-панель для управления ссылками
- Улучшение визуального дизайна
- Поддержка кастомных slug
- Оптимизация производительности
- Тестирование производительности
- Документация API (Swagger/OpenAPI)
- CI/CD пайплайн
- Мониторинг и логирование
- Деплой на облачные платформы
- Поддержка HTTPS
- Кэширование
- Масштабирование
- Резервное копирование
- Миграции базы данных
- Интернационализация
- Доступность (accessibility)
- SEO-оптимизация
- PWA (Progressive Web App)
- Офлайн-режим
- Интеграция с социальными сетями
- Экспорт данных
- Импорт данных
- API для программного доступа
- Webhooks
- Уведомления
- Поддержка различных форматов ввода и вывода, протоколов, доменов, путей и т.д.